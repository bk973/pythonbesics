# Are used to hold a list of Objects...
# Lists are mutable...
# Lists can be created by either calling the list() class or using literal []

my_list = list()
her_list = []

#adding elements to the list
list1 = ['money', 'apples', 'bags']
print(list1)


#Common methods that can be invoked on lists

# .append(element) adds item at the end of the list
# .insert(index, element) add an item to the list at a specified index
# .remove(element) remove an item from the list
# del <list>[index] removes an item from the list at a specified index
# .sort() Orders the values of the list in place... Doesn't return a new list, updates the existing list

