# Are immutable sequences
# Because tuples are immutable, you cannot append to them

#Creating a tuple
my_tuple = ('Cow', 'Goat');

empty = ()

#For tuples with one item, you need to append a comma (, ) after the item
one_item_tuple = (1,)